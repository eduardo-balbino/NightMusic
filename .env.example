# Optional: set this when the deployed frontend should proxy API calls to a
# separately deployed backend. Leave unset to keep only the built-in Next.js API
# routes active.
API_BASE_URL=https://api.example.com
